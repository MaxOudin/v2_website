<nav class="fixed top-0 z-50 w-full backdrop-blur-sm bg-white/10">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo -->
      <%= link_to "/", class: "flex-shrink-0 flex items-center" do %>
        V2Website
      <% end %>

      <!-- Menu pour mobile -->
      <div class="flex md:hidden">
        <button type="button" 
                class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-indigo-500"
                data-bs-toggle="collapse" 
                data-bs-target="#navbarSupportedContent" 
                aria-controls="navbarSupportedContent" 
                aria-expanded="false" 
                aria-label="Toggle navigation">
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>
      </div>

      <!-- Menu principal -->
      <div class="hidden md:flex md:items-center md:space-x-8" id="navbarSupportedContent">
        <%= link_to "Accueil", "/", class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
        <%= link_to "Projects", projects_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
        <%= link_to "Tech Stacks", tech_stacks_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
        
        <!-- Menu utilisateur -->
        <div class="ml-4 relative flex-shrink-0 group">
          <div>
            <button type="button" 
                    class="flex rounded-full bg-white focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
                    id="navbarDropdown"
                    data-bs-toggle="dropdown"
                    aria-expanded="false">
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Menu mobile (collapsed) -->
  <div class="md:hidden collapse" id="navbarSupportedContent">
    <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3 bg-white shadow-lg rounded-b-lg">
      <%= link_to "Accueil", "/", class: "block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-gray-900 hover:bg-gray-50 transition-colors duration-200" %>
      <%= link_to "Projects", projects_path, class: "block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-gray-900 hover:bg-gray-50 transition-colors duration-200" %>
      <%= link_to "Tech Stacks", tech_stacks_path, class: "block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-gray-900 hover:bg-gray-50 transition-colors duration-200" %>
    </div>
  </div>
</nav>

<!-- Spacer pour compenser la navbar fixe -->
<div class="h-16"></div>
