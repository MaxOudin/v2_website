<nav class="fixed top-0 z-50 w-full backdrop-blur-sm bg-white/10 h-[52px] md:h-16">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-[52px] md:h-16">
      <!-- Logo -->
      <%= link_to "/", class: "flex-shrink-0 flex items-center" do %>
        <%= image_tag "yellow_logo.svg", class: "w-10 h-10 rounded-full", alt: "Logo Maxime Oudin - Développeur Ruby on Rails Bordeaux" %>
        <span class="text-2xl text-gray-800 font-semibold ml-2">Maxime Oudin</span>
      <% end %>

      <!-- Actions visibles sur mobile -->
      <div class="flex md:hidden items-center space-x-2">
        <a href="#contact" class="px-3 py-2 rounded-lg text-sm font-semibold text-gray-900 bg-white border border-gray-300 hover:bg-gray-50 transition-colors">
          <%= t("navbar.contact") %>
        </a>
        <nav aria-label="Language selector" class="inline-flex rounded-md border border-gray-300 overflow-hidden">
          <%= link_to "FR", url_for(locale: :fr), class: "px-2 py-1 text-xs font-semibold #{I18n.locale.to_s == 'fr' ? 'bg-gray-900 text-white' : 'bg-white text-gray-700 hover:bg-gray-50'}" %>
          <%= link_to "EN", url_for(locale: :en), class: "px-2 py-1 text-xs font-semibold #{I18n.locale.to_s == 'en' ? 'bg-gray-900 text-white' : 'bg-white text-gray-700 hover:bg-gray-50'}" %>
        </nav>
      </div>

      <!-- Menu principal -->
      <div class="hidden md:flex md:items-center md:space-x-8" id="navbarSupportedContent">
        <%= link_to t("navbar.projects"), projects_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
        <%= link_to t("navbar.tech_stacks"), tech_stacks_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>

        <!-- CTA Desktop -->
        <div class="ml-4 flex items-center space-x-3">
          <a href="#contact" class="inline-flex items-center justify-center px-4 py-2 rounded-lg text-sm font-semibold text-white bg-gray-900 hover:bg-gray-800 transition-colors">
            <%= t("navbar.contact") %>
          </a>
          <nav aria-label="Language selector" class="hidden md:inline-flex rounded-md border border-gray-300 overflow-hidden">
            <%= link_to "FR", url_for(locale: :fr), class: "px-2 py-1 text-xs font-semibold #{I18n.locale.to_s == 'fr' ? 'bg-gray-100 text-gray-900' : 'bg-white text-gray-700 hover:bg-gray-50'}" %>
            <%= link_to "EN", url_for(locale: :en), class: "px-2 py-1 text-xs font-semibold #{I18n.locale.to_s == 'en' ? 'bg-amber-100 text-gray-900' : 'bg-white text-gray-700 hover:bg-gray-50'}" %>
          </nav>
        </div>
      </div>
    </div>
  </div>
</nav>

<!-- Spacer pour compenser la navbar fixe -->
<div class="h-[52px] md:h-16"></div>
