<% content_for :meta_product_name, DEFAULT_META["meta_product_name"] %>
<% content_for :meta_title, DEFAULT_META["meta_title"] %>
<% content_for :meta_description, DEFAULT_META["meta_description"] %>
<% content_for :meta_image, DEFAULT_META["meta_image"] %>

<!-- Hero Section -->
<main class="relative overflow-hidden" role="main">
  <!-- Section Hero -->
  <%= render "pages/hero" %>

  <!-- Section Perso -->
  <%# <%= render "pages/perso" %>

  <!-- Section Services -->
  <%# <%= render "pages/expertise" %>

  <!-- Section Services -->
  <%# <%= render "pages/services" %>

  <!-- Section Témoignages & Réalisations -->
  <%# <%= render "pages/testimonials" %>

  <!-- Section Contact -->
  <%# <%= render "pages/contact" %>
</main>
